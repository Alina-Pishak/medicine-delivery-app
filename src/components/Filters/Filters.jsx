import { Drawer } from "@mui/material";

const Filter = () => {
  return (
    <>
      <Drawer
        // anchor={anchor}
        open={true}
        // onClose={toggleDrawer(anchor, false)}
      >
        Hello
      </Drawer>
    </>
  );
};

export default Filter;
